import { PipeshortNamePipe } from './pipeshort-name-pipe';

describe('PipeshortNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeshortNamePipe();
    expect(pipe).toBeTruthy();
  });
});
